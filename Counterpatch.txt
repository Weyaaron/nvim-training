diff --git a/Readme.md b/Readme.md
index 135ea76..2a38e99 100644
--- a/Readme.md
+++ b/Readme.md
@@ -115,6 +115,7 @@ training.configure({
 	possible_marks_list = { "a", "b", "c", "r", "s", "t", "d", "n", "e" }, --A list of possible marks. (Optional, this is the default)
 	possible_register_list = { "a", "b", "c", "r", "s", "t", "d", "n", "e" }, -- A list of possible registers. (Optional, this the default)
 	audio_feedback = true, --Enables/Disables audio feedback, if enabled, requires the 'sox' package providing the 'play' command.
+	enable_counters = true, --Enables/Disables counters in tasks that support counters.
 })
 ```
 
diff --git a/lua/nvim-training/tasks/move_word.lua b/lua/nvim-training/tasks/move_word.lua
index d6ddcd5..a2f271c 100644
--- a/lua/nvim-training/tasks/move_word.lua
+++ b/lua/nvim-training/tasks/move_word.lua
@@ -1,5 +1,6 @@
 local utility = require("nvim-training.utility")
 local Task = require("nvim-training.task")
+local user_config = require("nvim-training.user_config")
 local MoveWord = {}
 MoveWord.__index = MoveWord
 setmetatable(MoveWord, { __index = Task })
@@ -7,49 +8,66 @@ MoveWord.__metadata = {
 	autocmd = "CursorMoved",
 	desc = "Move using w.",
 	instructions = "Move using w.",
-	tags = "movement, word, horizontal, w",
+	tags = "movement, word, horizontal, w, counter",
 }
 
 function MoveWord:new()
 	local base = Task:new()
 	setmetatable(base, { __index = MoveWord })
 	base.target_y_pos = 0
+	base.counter = 1
+	if user_config.enable_counters then
+		base.counter = math.random(2, 7)
+	end
 	return base
 end
 
 function MoveWord:activate()
 	local function _inner_update()
-		local cursor_at_line_start = false
-		local current_cursor_pos = vim.api.nvim_win_get_cursor(0)
-		while not cursor_at_line_start do
-			utility.set_buffer_to_rectangle_and_place_cursor_randomly()
-			current_cursor_pos = vim.api.nvim_win_get_cursor(0)
-			cursor_at_line_start = current_cursor_pos[2] < 15
-
-			local line = utility.get_line(current_cursor_pos[1])
-			local char_at_cursor_pos = line:sub(current_cursor_pos[2] + 1, current_cursor_pos[2] + 1)
-			if char_at_cursor_pos == " " then
-				cursor_at_line_start = false
-			end
-		end
+		-- local function check_cursor_pos()
+		-- 	local current_cursor_pos = vim.api.nvim_win_get_cursor(0)
+		-- 	cursor_at_line_start = current_cursor_pos[2] < 15
+		--
+		-- 	local line = utility.get_line(current_cursor_pos[1])
+		-- 	local char_at_cursor_pos = line:sub(current_cursor_pos[2] + 1, current_cursor_pos[2] + 1)
+		-- 	if char_at_cursor_pos == " " then
+		-- 		cursor_at_line_start = false
+		-- 	end
+		-- end
+		--
+		-- while not cursor_at_line_start do
+		-- 	utility.set_buffer_to_rectangle_and_place_cursor_randomly()
+		-- 	current_cursor_pos = vim.api.nvim_win_get_cursor(0)
+		-- end
+                
 
-		current_cursor_pos = vim.api.nvim_win_get_cursor(0)
+                
+		--Todo: This will stay the same
+		local current_cursor_pos = vim.api.nvim_win_get_cursor(0)
 		local line = utility.get_line(current_cursor_pos[1])
 		local word_positions = utility.calculate_word_bounds(line)
 		local word_index_cursor = utility.calculate_word_index_from_cursor_pos(word_positions, current_cursor_pos[2])
 
-		local offset = 0
+		local offset = self.counter
 		local cursor_is_at_wordend = current_cursor_pos[2] == word_positions[word_index_cursor][2]
 
 		if cursor_is_at_wordend then
 			offset = 1
 		end
-		self.target_y_pos = word_positions[word_index_cursor + 1 + offset][1]
+		self.target_y_pos = word_positions[word_index_cursor + offset][1]
 		utility.create_highlight(current_cursor_pos[1] - 1, self.target_y_pos, 1)
 	end
 	vim.schedule_wrap(_inner_update)()
 end
 
+function MoveWord:instructions()
+	local result = "Move using 'w'"
+	if user_config.enable_counters then
+		result = "Move " .. self.counter .. " words."
+	end
+	return result
+end
+
 function MoveWord:deactivate(autocmd_args)
 	return vim.api.nvim_win_get_cursor(0)[2] == self.target_y_pos
 end
diff --git a/lua/nvim-training/user_config.lua b/lua/nvim-training/user_config.lua
index 0932459..8ced0e8 100644
--- a/lua/nvim-training/user_config.lua
+++ b/lua/nvim-training/user_config.lua
@@ -4,6 +4,7 @@ local user_config = {
 	bracket_pairs = { { "(", ")" }, { "{", "}" }, { "[", "]" } },
 	audio_feedback = true,
 	custom_collections = {},
+	enable_counters = true,
 }
 
 return user_config
diff --git a/lua/nvim-training/utility.lua b/lua/nvim-training/utility.lua
index 4438de6..826e979 100644
--- a/lua/nvim-training/utility.lua
+++ b/lua/nvim-training/utility.lua
@@ -272,6 +272,10 @@ function utility.discard_tasks_by_tags(tasks, tag_list)
 	return tasks_with_tag
 end
 
+function utility.check_custom_collection()
+	--Todo: Implement check, like not 0, resolving
+end
+
 function utility.extract_text_between_cursor_and_target(start_indexes, end_indexes) end
 
 return utility
